<nav class="navbar navbar-expand-lg navbar-dark bg-dark h10 px-5 py-3 mb-5 shadow navItem">
    <div class="container-fluid ">
        <a class="navbar-brand" href="{{ path('app_home') }}">
{#             <img class="logoPrincipal" src="{{ asset('build/images/gabblerlogo.png') }}">#}

            Gabbler
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if app.user %}
                    <li  class="nav-item btn"> <a class="btn btn-success" href="{{ path('app_profil', {'value': 1}) }}"> My profil </a></li>
                {% else %}
                    <li class="nav-item btn"><a class="btn btn-success" href="{{ path('app_login') }}"> My profil </a>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li  class="nav-item btn"> <a class="btn btn-success" href="{{ path('app_user_shop') }}"> Subscribe </a></li>
                {% else %}
                    <li class="nav-item btn"><a class="btn btn-success" href="{{ path('app_login') }}"> Subscribe </a>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item btn text-end"><a class="btn btn-danger" href="{{ path('app_logout') }}">Logout</a></li>

                {% else %}
                    <li class="nav-item btn text-end"><a class="btn btn-success" href="{{ path('app_login') }}"> Sign In </a>
                    </li>
                {% endif %}



                {# <li  class="nav-item btn" data-export> <a class="btn btn-success" href="#"> Exporter les données </a></li> #}
            </ul>
            </ul>
        </div>
    </div>
</nav>